# Case-by-Case-method
def angle(a, b, x, y):
    u = np.array([a[0] - x, a[1] - y])
    v = np.array([b[0] - x, b[1] - y])
    return math.acos(np.dot(u, v) / (math.sqrt(np.dot(u, u)) * math.sqrt(np.dot(v, v))))
#두 점 a=(a1,a2),b=(b1,b2)와 중심의 x좌표와 y좌표를 넣었을 때, 
# 중심에서 두 점 a,b로 가는 두 벡터가 이루는 각을 구해주는 함수
# 넓이 공식을 이용하기 위해 기준이 되는 축의 점의 좌표와 위에서 구한 교점, 중심의 좌표를 넣어서 사용
